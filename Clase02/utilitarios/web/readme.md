# Utilitario de librerías

```
docker run -d node:18.19.1-alpine
docker run -it -d  node:18.19.1-alpine
docker exec -it keen_zhukovsky npm init
docker exec -it keen_zhukovsky npm install bootstrap
```

```
docker run -it -d -w /app -v ${PWD}/:/app node:18.19.1-alpine
docker exec -it sharp_rhodes npm init
docker exec -it sharp_rhodes npm install bootstrap
```
